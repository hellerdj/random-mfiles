function x = concat(a,b,fillval)% x = concat(a,b,fillval)% takes arrays a and b and stacks them one on the other.  The array% with the lower number of columns is filled with scalar fillval% to reach the same width as ma = size(a,2);mb = size(b,2);if nargin < 3  fillval = nan;endif ma == mb  x = [a;b];elseif ma < mb  x = [[a fillval*ones(size(a,1), mb-ma)]; b];else  x = [a; [b fillval*ones(size(b,1), ma-mb)]];end