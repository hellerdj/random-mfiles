function [lambda,llik] = poissfit_data(x)% [lambda,llik] = poissfit_data(x)%    finds maximum likelihood estimate of the rate parameters of a Poisson%    distribution that would account for obtaining the data in vector X.  In%    addition, the function returns the log likelihood of%    obtaining the data given the optimal parameter.%%    See llikdata_poisson.m %    M. Shadlen 9/94global DataData = x;% generate guessesq = mean(x)+1;				% guess is the meanlambda = fmins('llikdata_poisson',q);llik = -llikdata_poisson(lambda);