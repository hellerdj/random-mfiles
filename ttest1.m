function [t,p] = ttest(x,y)% TTEST performs single sample t-test.%	[t,p] = ttest1(x) returns the t- and p-values for whether the mean of x %	differs significantly from zero.  If x is an array, then the columns of x% 	are each tested against a mean of 0.%	[t,p] = ttest(x,y) returns the t- and p-vals for the means comparison.%if (nargin == 1)	df = length(x)-1;	sd = sqrt((std(x).^2)/df);	t = mean(x)./sd;else if (nargin == 2)	n1 = length(x);	n1 = length(y);	df = n1 + n2 - 2;	sd = sqrt((sum((x-mean(x)).^2) + sum((y-mean(y)).^2)) .* (1/n1 + 1/n2) ./ (n1 + n2 -2));	t = (mean(x)-mean(y))/sd;endp = betainc(df./(df+t.^2),df/2,.5);