function [yeven,yodd] = cauchy(x,x0,s,n)% CAUCHY	returns a pair of nth-order cauchy function of %		of x with parameter sigma = s. The axis of symmetry%		is at x0.  The even symmetric function is yeven, the odd % 	yodd. Usage: [yeven,yodd] = cauchy(x,x0,s,n)q = atan2(x - x0, s);q0 = cos(q);qn = q0;for i = 1:n-1		qn = qn .* q0;endyeven = qn .* cos(n * q);yodd = qn .* sin(n * q);