% playground for testing the hanes algorithm and comparing to Commenges and% Seal. % see hanes.mepochs = [.8 .6 1.2]s = make_exp_spikes([50 50 50], epochs);duration = sum(epochs)% if we want to make a ramp then we use the s vector below with the duration% preceding it.s = make_exp_spikes(linspace(5,200,20),.05*ones(20,1));duration=1       play_spikes(s,2); hold offplotopt = 1;[tb,tact,snumb,snumact,p] = hanes(s,[],plotopt)[rhatML,n,fr]=commseal(s)hold on; plot(rhatML,1,'c*')stem(s(2:length(s)),fr'/max(fr))s(1)psnumb