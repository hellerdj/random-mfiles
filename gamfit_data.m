function [order, m, llik] = gamfit_data(x,fp)% [order,m,llik] = gamfit_data(x)%    finds maximum likelihood estimates of parameters of the gamma%    distribution that would account for obtaining the data in vector X.  In%    addition to parameters, the function returns the log likelihood of%    obtaining the data given the optimal parameters.%%    See llikdata_normal.m  nfit_data llikdata_gamma%    M. Shadlen 9/94global DataData = x;% generate guessesq(2) = mean(x);q(1) = 2;if nargin > 1  if ~isnan(fp(1)), q(1) = fp(1), end;  if ~isnan(fp(2)), q(2) = fp(2), end;endg = fmins('llikdata_gamma',q);m = g(2);order = g(1);llik = llikdata_gamma(g);