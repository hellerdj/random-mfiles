function sid = getsid(paranum,e0)%  sid = getsid(paranum,e0)% returns the stimulus id (sid) from the snippet of efile, E0% 10/29/96 mil & mns wrote it% 8/1/97 added 602 capacity. jdr & mns% 5/12/98 jpg fixed the reset-state bug - routine now returns NaN instead of% emptyif paranum == 512  q = find(e0(:,1) > 5000 & e0(:,1) < 5100 );  sid = e0(q,1);elseif ismember(paranum, [602 502 522])   q = find(e0(:,1) > 5000 & e0(:,1) < 5100 );  sid = e0(q,1)-5000; else  error('getsid: bad paradigm number')endif isempty(sid), sid=NaN; end % sometimes when the rex user resets state, there will be a trial without a% stim id. This would cause this routine to return an empty, and routines% calling this routine would crash.