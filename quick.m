function p = quick(x,alpha,beta)%  p = quick(x,alpha,beta) coputes quick function at values in x given%  parameters alpha & beta.  This version assumes a guessing rate of 0.5% returns vector or matrix, p, the same size as x. If x is a matrix, then % the function operates on each of the columns. alpha & beta must be% scalars.% see also quickfitif ~isscalar(alpha) & ~isscalar(beta)  error('alpha and beta must be scalars')endp = 1 - 0.5 * exp(-(x/alpha).^beta);