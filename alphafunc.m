function a = alphafunc(t,tg,td, nonormalize)% a = alphafunc(t,tg,td)% computes alpha function of time, t, given growth and decay time constants,% tg, td.  T can be a scalar or vector   If t is a vector, then The function% is normalized to area = 1 unless nonnormalize is set to 1.% % 5/28/98 mns wrote itif nargin < 3  error('alphafunc requires three args.  Type help alphafunc.')endif ~isscalar(tg) | ~isscalar(td)  error('time constants must be scalar values')endif nargin < 4  nonormalize = 0;endif isscalar(t)  a = (1-exp(-t./tg)) .* exp(-t./td);else  % t is a vector  q = (1-exp(-t./tg)) .* exp(-t./td);  if nonormalize==1    a = q;  else    a = q ./ trapz(t,q);  endend