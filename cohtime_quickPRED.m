function p = cohtime_quickPRED(c,t,alpha,beta,tau)%   Computes a special version of the Quick (integral Weibul) function of%   probability correct versus coh and time. The function is%   parameterized by 3 values: alpha, beta and tau.  This routine returns %   predicted values, p, for coherence, c and time t.  These must either%   be passed as identically sized vectors/matrices. Alternatively, one%   can be a scalar.  This facilitates computing the PMF at a particular%   value of time.%%  SYNTAX: p = cohtime_quickPRED(c,t,alpha,beta,tau)%% see also cohtime_quick.mif any(size(c)~=size(t))  if isscalar(c)    c = repmat(c,size(t));  elseif isscalar(t)    t = repmat(t,size(c));  else    error('inputs must be same size, or at least one must be scalar')  endend    if tau<=0  w = 1; else  w = 1 - exp(-t/tau);endp = 1 - 0.5 * exp(-((w.*c / alpha).^beta));