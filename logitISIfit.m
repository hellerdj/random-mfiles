function [beta,llik,pred] = logitISIfit(data)% Not ready yet.   Just an idea . . .%   data is a vector of spike arrival times.  The idea is to fit a sigmoid%   to them with 4 parameters: b1 + b2 * logistic(b3 + b4*t)%   b1 is the pedestal rate. b2 is a scaling factor. b3 is %   %	usage: [beta,llik,pred] = logitISIfit(data)%	Return values are%	   BETA, the coefficients corresponding to%	   LLIK, the log likelihood of the fit%	   PRED, a vector representing the prob(y=1) under the fit.%global Data;Data = data;